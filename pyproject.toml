[project]
name = "reasonova-prototype"
version = "0.1.0"
description = "Internship readiness expert system prototype for WID2001."
requires-python = ">=3.12"
dependencies = [
  "experta==1.9.4",
  "Flask==3.1.2",
]

[tool.vercel]
entrypoint = "app:app"
